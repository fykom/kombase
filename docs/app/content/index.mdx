---
title: Introduction
description: Customizable Shadcn component and wrapper form validation with RHF and zod
---

## Overview

`komdes` is a centralized UI library built on top of Radix UI primitives (shadcn/ui pattern), `react-hook-form`, and `@tanstack/react-table`. It is designed for consistency and reusability across multiple frontend projects.

## Installation

<Steps>
<Step>
### Install shadcn/ui

Ensure you have installed and configured [shadcn/ui](https://ui.shadcn.com/docs/installation) in your project. `komdes` relies on the shadcn component architecture for UI consistency.

</Step>

<Step>
### Install komdes

Run the following command using your preferred package manager:

```package-install
komdes
```

</Step>
</Steps>
