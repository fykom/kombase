---
title: FormRadioGroup
description: renders selectable options as **clickable cards**. It natively supports `as const` arrays and custom rendering.
preview: false
---

## FormRadioGroup

<ComponentTabs name="form-radio-group-demo" align="start" className="p-4" />

### FormRadioGroupProps

<AutoTypeTable
  path="../types/form-radio-group.types.ts"
  name="FormRadioGroupProps"
/>

### `RadioOption` Type

<AutoTypeTable path="../types/form-radio-group.types.ts" name="RadioOption" />

### Custom Render Slot

Override the default card using `renderOption`:

```tsx
<FormRadioGroup
  control={form.control}
  name="gateway"
  options={GATEWAYS}
  renderOption={(opt, isSelected) => (
    <div
      className={cn("p-4 border rounded-lg", isSelected && "border-primary")}
    >
      {opt.label}
    </div>
  )}
/>
```
